import { Inter } from 'next/font/google';
import { lusi } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const lusi = lusi({ weight: ['400', '700'], subsets: ['latin'] });